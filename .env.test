# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# DATABASE_URL is injected as a real env var by compose.yaml inside Docker,
# so it takes precedence over any .env file — no need to duplicate it here.
# When running tests outside Docker, create .env.test.local with your local URL.


# Mercado Pago (stubs — no real calls in tests)
MERCADOPAGO_ACCESS_TOKEN=TEST-stub-access-token
MERCADOPAGO_APP_ID=12345678
MERCADOPAGO_APP_SECRET=test-app-secret
MERCADOPAGO_OAUTH_REDIRECT_URI=http://localhost/oauth/mercadopago/callback
MERCADOPAGO_WEBHOOK_SECRET=test-webhook-secret
MERCADOPAGO_MARKETPLACE_FEE_PERCENT=5.0

# Token encryption — 32-byte key, base64-encoded
TOKEN_ENCRYPTION_KEY=WFkmDonf2/TL4kAwakWJyL3bbwEzEuY6ET/vXSNmZ2Q=

# Mercure (hub not running in tests; token factory still needs the secret)
MERCURE_JWT_SECRET=test-mercure-secret
MERCURE_URL=http://localhost/.well-known/mercure
MERCURE_PUBLIC_URL=http://localhost/.well-known/mercure
