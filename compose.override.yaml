---
# Development environment override
services:
  traefik:
    restart: "no"
    command:
      - --providers.docker=true
      - --providers.docker.exposedByDefault=false
      - --entrypoints.web.address=:80
      # Dashboard enabled for local development only
      - --api.dashboard=true
      - --api.insecure=true
    ports:
      - "0.0.0.0:80:80"
      - "127.0.0.1:8080:8080"

  php:
    restart: "no"
    build:
      context: .
      target: frankenphp_dev
    volumes:
      - ./:/app
      - ./frankenphp/Caddyfile:/etc/frankenphp/Caddyfile:ro
      - ./frankenphp/conf.d/20-app.dev.ini:/usr/local/etc/php/app.conf.d/20-app.dev.ini:ro
      # If you develop on Mac or Windows you can remove the vendor/ directory
      #  from the bind-mount for better performance by enabling the next line:
      #- /app/vendor
    environment:
      FRANKENPHP_WORKER_CONFIG: watch
      MERCURE_EXTRA_DIRECTIVES: demo
      # See https://xdebug.org/docs/all_settings#mode
      XDEBUG_MODE: "${XDEBUG_MODE:-off}"
      APP_ENV: "${APP_ENV:-dev}"
    extra_hosts:
      # Ensure that host.docker.internal is correctly defined on Linux
      - host.docker.internal:host-gateway
    tty: true
###> symfony/mercure-bundle ###
###< symfony/mercure-bundle ###

###> doctrine/doctrine-bundle ###
  database:
    restart: "no"
    ports:
      - "3306:3306"
###< doctrine/doctrine-bundle ###

###> symfony/mailer ###
  mailer:
    restart: "no"
    image: axllent/mailpit
    ports:
      - "1025"
      - "8025"
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1
###< symfony/mailer ###

  rabbitmq:
    restart: "no"
    # Direct port mappings removed — management UI accessible via Traefik at http://rabbitmq.localhost
    # AMQP port 5672 is accessible internally via Docker networking

  redis:
    restart: "no"
    ports:
      - "6379:6379"

  opensearch:
    restart: "no"

  opensearch-dashboards:
    restart: "no"

  # Messenger worker services — development overrides
  #
  # Source is bind-mounted so code changes are reflected immediately
  # without rebuilding the image, consistent with the php service behaviour.

  messenger_worker:
    restart: unless-stopped
    build:
      context: .
      target: frankenphp_dev
    volumes:
      - ./:/app
    environment:
      APP_ENV: "${APP_ENV:-dev}"

  messenger_worker_webhooks:
    restart: unless-stopped
    build:
      context: .
      target: frankenphp_dev
    volumes:
      - ./:/app
    environment:
      APP_ENV: "${APP_ENV:-dev}"

  messenger_worker_tracking:
    restart: unless-stopped
    build:
      context: .
      target: frankenphp_dev
    volumes:
      - ./:/app
    environment:
      APP_ENV: "${APP_ENV:-dev}"
