---
name: CI

on:
  push:
    branches:
      - main
  pull_request: ~
  workflow_dispatch: ~

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  tests:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker images
        uses: docker/bake-action@v6
        with:
          pull: true
          load: true
          source: .
          files: |
            compose.yaml
            compose.override.yaml
          set: |
            *.cache-from=type=gha,scope=${{github.ref}}
            *.cache-from=type=gha,scope=refs/heads/main
            *.cache-to=type=gha,scope=${{github.ref}},mode=max

      - name: Install Composer dependencies
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/app \
            -w /app \
            -e COMPOSER_ALLOW_SUPERUSER=1 \
            --entrypoint="" \
            app-php \
            php -d memory_limit=-1 /usr/local/bin/composer install \
              --prefer-dist --no-progress --no-interaction --no-scripts

      - name: Install ImportMap assets
        # Soluciona el error: "The @hotwired/stimulus vendor asset is missing"
        # ImportMap requiere descargar los assets JS que no están en el repo.
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/app \
            -w /app \
            --entrypoint="" \
            app-php \
            php bin/console importmap:install

      - name: Generate JWT keys
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/app \
            -w /app \
            --entrypoint="" \
            app-php \
            php bin/console lexik:jwt:generate-keypair --skip-if-exists

      - name: Verify Symfony console
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/app \
            -w /app \
            --entrypoint="" \
            app-php \
            php bin/console -V

      - name: Start services
        run: |
          docker compose up --wait --no-build || {
            echo "=== PHP container logs ==="
            docker compose logs --no-color php
            exit 1
          }

      - name: Check HTTP reachability
        run: curl -v --fail-with-body http://localhost

      - name: Check Mercure reachability
        run: curl -vI --insecure --fail-with-body https://localhost/.well-known/mercure?topic=test

      - name: Create test database
        run: |
          docker compose exec -T database \
            mysql -uroot -p"${MYSQL_ROOT_PASSWORD:-passwd}" \
            -e "CREATE DATABASE IF NOT EXISTS db_test CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
                GRANT ALL PRIVILEGES ON db_test.* TO 'user'@'%';
                FLUSH PRIVILEGES;"

      - name: Run migrations
        run: docker compose exec -T php bin/console -e test doctrine:migrations:migrate --no-interaction

      - name: Run PHPUnit
        run: docker compose exec -T php bin/phpunit

      - name: Doctrine Schema Validator
        run: docker compose exec -T php bin/console -e test doctrine:schema:validate

  linter:
    name: Symfony Lint (PHP 8.5 - FrankenPHP)
    runs-on: ubuntu-latest

    container:
      image: dunglas/frankenphp:1-php8.5

    env:
      APP_ENV: test
      SYMFONY_DEPRECATIONS_HELPER: weak

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          apt-get update
          apt-get install -y git unzip

      - name: Install PHP extensions
        run: |
          install-php-extensions \
            @composer \
            intl \
            pdo_mysql \
            zip \
            opcache

      - name: Install dependencies (no dev autoload yet)
        run: |
          composer install \
            --no-interaction \
            --no-progress \
            --prefer-dist \
            --no-scripts

      - name: Dump optimized autoload
        run: |
          composer dump-autoload --classmap-authoritative

      - name: Lint container
        run: php bin/console lint:container

      - name: Lint YAML
        run: php bin/console lint:yaml config --parse-tags

      - name: Lint Twig
        run: php bin/console lint:twig templates

      - name: Lint PHP
        run: find src -type f -name "*.php" -print0 | xargs -0 -n1 php -l
